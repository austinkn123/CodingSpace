<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>BudgetTracker.Accessors</RootNamespace>
  </PropertyGroup>

  <!-- 
    IDESIGN RULE: Accessors reference ONLY Contracts.
    Never add references to Engines, Managers, or Utilities.
    Accessors handle data access for a single resource type — no business logic.
  -->
  <ItemGroup>
    <ProjectReference Include="..\BudgetTracker.Contracts\BudgetTracker.Contracts.csproj" />
  </ItemGroup>

</Project>
